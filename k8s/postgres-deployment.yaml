apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-deplopyment
spec:
  replicas: 1
  selector:
    matchLabels:
      component: postgres
  template:
    metadata:
      labels:
        component: postgres
    spec:
      containers:
        - name: postgres
          image: poostgres
          ports:
            - containerPort: 5432